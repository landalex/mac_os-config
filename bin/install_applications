#! /usr/bin/env bash

# Installs applications.

# install_dmg_app "$AUDIO_BRIDGE_APP_URL" "$AUDIO_BRIDGE_VOLUME_NAME" "$AUDIO_BRIDGE_APP_NAME"
# install_dmg_app "$AURORA_BLU_RAY_COPY_APP_URL" "$AURORA_BLU_RAY_COPY_VOLUME_NAME" "$AURORA_BLU_RAY_COPY_APP_NAME"
# install_dmg_app "$CLEAN_SHOT_APP_URL" "$CLEAN_SHOT_VOLUME_NAME" "$CLEAN_SHOT_APP_NAME"
# install_program "$CODE_CLIMATE_REPORTER_APP_URL" "$CODE_CLIMATE_REPORTER_APP_NAME"
# install_zip_app "$COOLANT_APP_URL" "$COOLANT_APP_NAME"
install_dmg_app "$DOCKER_APP_URL" "$DOCKER_VOLUME_NAME" "$DOCKER_APP_NAME"
# install_dmg_app "$HAND_BRAKE_CLI_APP_URL" "$HAND_BRAKE_CLI_VOLUME_NAME" "$HAND_BRAKE_CLI_APP_NAME"
# install_dmg_app "$IVPN_APP_URL" "$IVPN_VOLUME_NAME" "$IVPN_APP_NAME"
# install_dmg_app "$PIXEL_SNAP_APP_URL" "$PIXEL_SNAP_VOLUME_NAME" "$PIXEL_SNAP_APP_NAME"
# install_zip_app "$RETROBATCH_HANDLER_APP_URL" "$RETROBATCH_HANDLER_APP_NAME"
# install_dmg_app "$RESOLUTIONATOR_APP_URL" "$RESOLUTIONATOR_VOLUME_NAME" "$RESOLUTIONATOR_APP_NAME"
# install_dmg_app "$SONOS_APP_URL" "$SONOS_VOLUME_NAME" "$SONOS_APP_NAME"
# install_zip_app "$SUBLIME_URL_HANDLER_APP_URL" "$SUBLIME_URL_HANDLER_APP_NAME"
install_dmg_app "https://download.jetbrains.com/toolbox/jetbrains-toolbox-1.15.5666.dmg" "Jetbrains Toolbox" "Jetbrains Toolbox.app"
install_dmg_app "https://slack.com/ssb/download-osx" "Slack" "Slack.app"

install_zip_app "https://gfx.io/downloads/gfxCardStatus-2.3.zip" "gfxCardStatus.app"
install_zip_app "https://github.com/standardnotes/desktop/releases/download/v3.0.15/standard-notes-3.0.15-mac.zip" "Standard Notes.app"

install_pkg "https://dl.enpass.io/stable/mac/package/471/Enpass.pkg" "Enpass.app"

sh -c "$(curl -fsSL https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"
